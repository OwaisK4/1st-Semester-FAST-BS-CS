#include <stdio.h>
/*
int main(){
	int a,b,c = 0;
	int d = 5&&c!=8||!c;
	printf("%d\n",d);
	a=b=c=-3%-8/2+7;
	printf("%d %d %d\n",a,b,c);
	return 0;
}
main(){ 
	int i = 4, j = -1, k = 0, w, x, y, z ;
	w = i || j || k ;
	x = i && j && k ;
	y = i || j && k ;
	z = i && j || k ; 
	printf ( "\nw = %d x = %d y = %d z = %d\n", w, x, y, z ) ;
}
main(){
	int i = 2, j = 3, k, l ;
	float a, b ;
	k = i / j * j ;
	l = j / i * i ;
	a = i / j * j ;
	b = j / i * i ;
	printf( "%d %d %f %f\n", k, l, a, b );
	}
	*/
int main(){ 
	/*
	int i = 4, j = -1, k = 0, w, x, y, z ;
	w = i || j || k ;
	x = i && j && k ;
	y = i || j && k ;
	z = i && j || k ; 
	printf ( "\nw = %d x = %d y = %d z = %d\n", w, x, y, z ) ;
	
	
	int i = 2, j = 3, k, l ;
	float a, b ;
	k = i / j * j ;
	l = j / i * i ;
	a = i / j * j ;
	b = j / i * i ;
	printf( "%d %d %f %f\n", k, l, a, b );
	
	int m,n;
	m = -3%2!=!3 ;
	n = -3*10.5/2 - 3 ;
	printf ( "a = %d\nb = %f", m, n ) ;
	*/
	int c = 0;
	int d = 5&&c!=8||!c;
	printf("%d\n",d);

}
