#include <stdio.h>
#include <string.h>

int main(){
	char a[] = "owaisalikhan";
	char b[100];
	int i;
//	printf(strstr(a, "ali"));
//	printf(strchr(a, 'k'));
	int size = strlen(a);
//	strncpy(b, a, );
//	printf("Size = %d", size);
//	printf("%s",b);
	for (i=65; i<91; i++){
		printf("%c\n", i);
	}
	return 0;
}
